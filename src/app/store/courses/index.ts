import * as types from './courses.types';
import * as actions from './courses.actions';
import * as selectors from './courses.selectors';
import { reducer, State, featureKey, initialState } from './courses.reducer';

export {
  types,
  actions,
  reducer,
  selectors,
  initialState,
  State,
  featureKey
};
