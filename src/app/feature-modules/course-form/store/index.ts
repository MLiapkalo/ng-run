import * as types from './course-form.types';
import * as actions from './course-form.actions';
import * as selectors from './course-form.selectors';
import { reducer, State, featureKey, initialState } from './course-form.reducer';

export {
  types,
  actions,
  reducer,
  selectors,
  initialState,
  State,
  featureKey
};
